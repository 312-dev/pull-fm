# Copy to backend.hcl (gitignored) and fill in.
#   terraform init -backend-config=backend.hcl

bucket = "pull-fm-tfstate"
key    = "shared/terraform.tfstate"
region = "auto"

endpoints = {
  # NO `.eu.` HERE, DELIBERATELY. An R2 jurisdiction endpoint only sees buckets
  # created in that jurisdiction, and `pull-fm-tfstate` is not EU-pinned
  # (verified 2026-07-29 by enumerating both endpoints). The `.eu.` form this
  # example used to carry fails at `terraform init` with a missing-bucket error.
  # The backups bucket IS EU-pinned; that is a different bucket. See
  # PULLFM-RISK-010.
  s3 = "https://REPLACE_WITH_CLOUDFLARE_ACCOUNT_ID.r2.cloudflarestorage.com"
}

use_lockfile = true

skip_credentials_validation = true
skip_region_validation      = true
skip_requesting_account_id  = true
skip_metadata_api_check     = true
skip_s3_checksum            = true
use_path_style              = true
